APP_NAME="Queue Management System"
APP_ENV=local
APP_KEY=base64:mtDiemcFuU6i+k6ssUDWqaNL3ZOHWFU0nhB9WohsSwc=
APP_DEBUG=false
APP_TIMEZONE=Asia/Manila
APP_URL=https://qms.sogodlgu.gov.ph

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
APP_MAINTENANCE_STORE=database

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=brcmjioi_qms
DB_USERNAME=brcmjioi_toldz
DB_PASSWORD=Sogodlgu2019

# Recommended for shared hosting (fast + simple)
SESSION_DRIVER=file
SESSION_LIFETIME=480
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIES=true
SESSION_SAME_SITE=lax

# Optional: only if you want sessions stored in DB instead
# SESSION_DRIVER=database

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync

# Recommended for shared hosting (fast + simple)
CACHE_STORE=file
CACHE_PREFIX=

# Optional: only if you want cache stored in DB instead
# CACHE_STORE=database

# Optional (only if you specifically configured custom paths)
# SESSION_FILES_PATH=/full/server/path/to/sessions
# CACHE_FILE_PATH=/full/server/path/to/cache
# CACHE_LOCK_PATH=/full/server/path/to/cache

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379
REDIS_CACHE_CONNECTION=cache

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# Broadcasting (Optional - for real-time updates)
# BROADCAST_DRIVER=pusher
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_APP_CLUSTER=mt1